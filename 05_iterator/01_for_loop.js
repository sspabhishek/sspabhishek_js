// for
const array = ["Iron Men", "Spider Men", "Thor", "Dead pool"]
for (let i = 0; i < array.length; i++) {
    const element = array[i];
    console.log(element);    
}

// all basics are same as cpp or c
// break
// nested loop
// continue etc